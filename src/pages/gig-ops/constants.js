// Shared by the Gig Ops page and its tabs. Lives here rather than in
// GigOpsMissionControl.jsx so the tabs don't have to import from their own
// parent (a circular import).
export const GIG_TRACKER_URL = 'https://gig-tracker-lemon.vercel.app/';
